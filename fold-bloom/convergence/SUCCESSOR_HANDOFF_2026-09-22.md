# FOLD//BLOOM — SUCCESSOR HANDOFF
## LIVE 0.13 + EXPERIENCE SET 0.1 / 2026-09-22

This file is for future ChatGPT / Hermes / a human returning cold.

## RECOVER FIRST

Authoritative current merged family:
- LIVE 0.13 AUTOPILOT polish — PR #151 · merge ce475fe2f9abca2068032c3dc4bdf236bcf0ee45 · CI 35705196976 PASS
- LISTEN 0.6 source-bundle + source-hash ride tuning
- GLYPH ATLAS 0.2 inspectable source memory
- TWO DIAL 0.10.3 composition authority
- EXPERIENCE SET 0.1 pure higher-order journey object — PR #152 · merged and CI green
- ECOLOGY 0.2 persistent lineage/game

Read in this order:
1. `/control/CURRENT.json#current_heads[fold-bloom]`
2. `/fold-bloom/live/release.json`
3. `/returns/FOLD_BLOOM_VISUAL_WORLD_2026-09-22.json`
4. `/fold-bloom/convergence/EXECUTION_ROADMAP_2026-09-22.md`
5. `/fold-bloom/convergence/EXPERIENCE_COMPILER_SPEC_0.1.md`
6. this file

Do not reconstruct the architecture from screenshots or names.

## WHAT THE USER ACTUALLY LIKES

Direct embodied returns:
- LIVE is already delightful enough that the user expects it to become part of lived experience.
- Movement/jitter/POV can be felt somatically even with low visual resolution.
- The user wants rare huge consequences: buildup → tunnel/compression → massive plunge → open sky/release.
- 0.11's increased transparency was a regression; 0.12 restored solidity and felt directionally correct.
- Visual worlds should be materially different, sophisticated and artful, not merely synth presets.
- Lyrics/text were exciting; sync needs explicit calibration.
- The user repeatedly wants a normie to touch the artifact and immediately understand/delight without learning our ontology.
- The user wants eventual playlists/sets, curation, photos/memories, podcasts, documents, daylines and communications—not as separate sovereign apps, but as unequal projections over addressed linear sources.
- The user uses nervous-system reports as a real promotion gate. Do not substitute internal metrics for direct feel.

## CORE MODEL — DO NOT FLATTEN

```
SOURCE → MAP → PROFILE → PROJECTION → HUMAN OPERATION → RETURN
```

- SOURCE = authority / exact identity.
- MAP = derived evidence.
- PROFILE = human calibration/taste, never truth.
- PROJECTION = LIVE / LISTEN / ATLAS / READ / etc.
- HUMAN OPERATION = authored meaning/world-writing.
- RETURN = durable path/packet/experience receipt.

The ring is a cyclic control/state projection against a linear source address. It is not the source timeline itself.

## 0.13 MERGED DELTA

This pass is intentionally polish/performance/succession, not new ontology.

### Human contact
- public name AUTOPILOT replaces IDLE RIDE; internal witness/no-authorship law remains.
- hard-edge AUTOPILOT/TAKE OVER control is reachable without opening settings.
- phone settings become safe-area bounded, scrollable and touch-pan enabled.
- minimum action targets are ~44px.

### Jitter / performance
- local and practice TRACKFIELD model rebuilds are bounded to ~36 Hz rather than nearly every RAF.
- canvas still renders at RAF cadence.
- projected road geometry receives display-only exponential damping.
- fresh semantic witnesses (beat/phrase/section/deformation/event identity) are not smoothed away.
- timed text lookup uses binary search for long-form/podcast-scale cue sets.
- DOM HUD/perf writes are throttled to ~10 Hz.
- runtime exposes a bounded FPS/model-slice witness.

### Aesthetics
- scene colors morph in OKLab rather than snapping.
- section variation uses golden-ratio phase to avoid short repeating visual cycles.
- world morph/variation is projection-only.

## NON-NEGOTIABLE LAWS

- RECOVER BEFORE INVENTING.
- GEOMETRY = MATTER; EFFECTS = ATMOSPHERE.
- DEFAULT TERRAIN OCCLUDES ITSELF.
- RARE LARGE EVENTS > CONSTANT SPECTACLE.
- ONE SOURCE EVENT = ONE BODY EVENT.
- BYTE IDENTITY != ORIGIN != DISPLAY META != TEXT != COLLECTION != HUMAN PATH.
- PROFILE MUTATES PROJECTION, NOT MAP EVIDENCE.
- DISPLAY SMOOTHING MUST NOT MUTATE SOURCE TIME OR EVENT EVIDENCE.
- AUTOPILOT BORROWS MOTION, NEVER AUTHORSHIP.
- TIMED TEXT MAY CLAIM ALIGNMENT; UNTIMED TEXT MUST NOT.
- MULTISENSORY OVERLOAD IS LAWFUL ONLY WHEN CHANNELS SHARE ONE ADDRESSED CAUSE.
- FX = WITNESS, NOT REWARD.
- Do not grow a combo economy unless direct use proves it means something.
- Do not clone LIVE racer visuals into Ecology or READFIELD.

## CURRENT NEXT EXECUTABLE — DO NOT REDESIGN IT

EXPERIENCE SET v0.1 is **done**. PR #152 merged the pure schema/codec/fixture/tests; PR #154 sealed merge state.

The bounded sequence now resumes at:

1. **SET COMPOSITOR 0.1 / block-Tetris authoring surface**
2. JOURNEY RIDE 0.1 / continuous multi-source traversal
3. SOURCE SHELL 0.1 / RIDE · SEE · SHAPE
4. PODCAST ADAPTER 0.1 / first non-music proof

Use:
- `/fold-bloom/experience-set/`
- `/fold-bloom/convergence/EXECUTION_ROADMAP_2026-09-22.md`
- `/fold-bloom/convergence/EXPERIENCE_COMPILER_SPEC_0.1.md`

### Recovered Design/Fold–Bloom delta

A later chat branch added useful intent that was not yet durable in this handoff:

- **LISTEN 0.7 is now the source editor candidate**: MAP evidence, MARK meaning, ALIGN temporal binding, SHAPE ride/profile.
- **READFIELD 0.8 now projects the same linked source address** with explicit one-way FOLLOW AUDIO, dual audio/text heads, and shared MARK; manual reader action releases follow.
- **field-source-spine/v0.1** is the fading continuity witness across LISTEN / LIVE 0.13 / READFIELD; it is not a new app or timeline authority.
- **field-text-alignment/v0.1** implements anchor-based correction beside immutable cue evidence: one anchor fixes arbitrary offset, two+ fix drift; coarse nudge is secondary.
- LISTEN’s SOURCE HORIZON adds low-alpha scoped audio-feature traces as ambient orientation from the same map evidence.

Do not create a SOURCE SPINE app or merge projection authorities. Direct problem-track QA is the runtime gate; SET COMPOSITOR 0.1 remains the next journey executable.

0.13 body QA may still interrupt this sequence if material/event coherence, reachability or response regresses.

## CONNECTIONS WORTH PRESERVING

- LISTEN ↔ LIVE: same source hash, pins and RIDE PROFILE; analysis stays authoritative.
- ATLAS ↔ source: glyph is thumbnail/projection of same source, not a new identity.
- READFIELD: transfer canonical-address/projection law, not music metrics/racer skin.
- AXIAL / Lens: donor for object→aperture→projection→RETURN discipline.
- HUMAN PORT / comms: promising transfer is ADDRESS + ANNOTATION + PROJECTION + RETURN, not score/combo logic.
- Sleeper / path systems: donor relation is deterministic source→world→traversal→RETURN, not identity merge.
- Photos/memories/dayline: future adapters should preserve exact source/media identity and canonical order before visual inference.

## NEXT HUMAN QA FORMAT

Prefer tiny reports:
- AUTOPILOT: REACHABLE / NOT
- MENU: ALL CONTROLS REACHABLE / NOT
- JITTER: BETTER / SAME / WORSE
- RESPONSE: CRISP / TOO SMOOTH
- DROP: MISSED / EARLY / LATE / RIGHT / DOUBLE
- WORLDS: DISTINCT / MEH
- TEXT: OFFSET ~ ___ s
- FPS witness only if visible stutter remains

## SUCCESSION RULE

Do not spend a turn explaining this file back to the user if an obvious bounded repair or implementation can be executed. Recover current head, perform one meaningful delta, verify, RETURN, then replan.
