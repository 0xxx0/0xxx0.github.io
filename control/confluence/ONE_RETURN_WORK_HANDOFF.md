# ONE RETURN v2 / WORK HANDOFF

**Target:** polished **SLEEPER // ONE RETURN** Work-mode build  
**Historical host:** `https://sleeper-one-return.metaname.chatgpt.site/one-return`  
**Historical workspace evidence:** `/workspace/sites/sleeper-one-return/`  
**Design contract evidence:** `/workspace/sites/sleeper-one-return/docs/ONE_RETURN_V2.md`

## What to share

Best handoff, in descending order:

1. **Work project/source export** — zip/folder containing source + package/config + docs + tests.
2. If Work only exposes selected files, send at minimum:
   - `docs/ONE_RETURN_V2.md`
   - application source (`src/` or equivalent)
   - package/build metadata
   - any test/fixture files mentioning phrase/cell/figure, witness, world key, figures, Gates, City/Disc, Return
3. If the old app can still be opened but source cannot be exported:
   - save/download the page/app bundle if Work offers it;
   - copy the full reproducible/deep-link URL for at least **two different FIGURE choices**;
   - export one Witness/Return JSON from each;
   - capture one screenshot of the figure selector and one in-world screenshot.
4. Screenshots alone are fallback evidence, not the preferred recovery object.

## Do not normalize before sharing

Preserve:
- original filenames and directory structure;
- URL/query string exactly as generated;
- figure names and order exactly;
- sample phrase/cell inputs;
- any seed/worldKey/witness identifiers;
- existing docs and test fixtures even if they appear obsolete.

Do **not** rename the figures or convert Conch/Keris/W8/Spiral into later operator labels before import.

## Known target contract

```
PHRASE + CELL + FIGURE
          ↓
  deterministic WORLD
          ↓
       GATES
     CITY / DISC
          ↓
 WITNESS / RETURN
```

Recovered distinctions:
- **PHRASE** affects world topology / passages / Gate placement / wall alphabet / world identity.
- **CELL** affects carried material, Gate order, governing law and provenance.
- **FIGURE** affects movement/protocol physics — this is the defining discriminator from the ASCII reconstruction.
- reproducible world/deep links and Witness import/export existed.

## Import rule

When the real project arrives:

1. preserve it first under a frozen recovery/source shelf;
2. run it without rewriting;
3. identify exact figure picker + URL serializer + witness schema;
4. register the polished app as the ONE RETURN head only after runtime verification;
5. keep `/sleeper/one-return/` ASCII reconstruction as a related donor unless the recovered v2 itself proves a direct migration path.

## CONFLUENCE boundary

CONFLUENCE may extract mechanisms from ONE RETURN, but must not absorb the app.

Likely transfer candidates after recovery:
- FIGURE as lawful physics/protocol projection over one world identity;
- reproducible world address;
- Gate proof/evidence relation;
- City ↔ Disc view coordination;
- Witness/Return replay;
- Phrase/Cell as unequal source compilers.

The current ASCII donor and Grid Path compiler are adjacent comparisons, not substitutes for the Work project.
