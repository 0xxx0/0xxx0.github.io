# ATLAS DAYLINE / MOBILE ACTION — 2026-09-25

## Trigger

Direct lived report: Dayline was not functionally usable/viewable on mobile.

That observation supersedes the earlier source-only responsive claim. Meanwhile PR #259 landed the correct real-life composition head: HOUSE / SHOPPING / COMMS lend explicit session-local context/tasks into Dayline while native sources retain authority.

## Current composition

`HOUSE / SHOPPING / COMMS → atlas-dayline-handoff/v0.1 → explicit APPLY/ADD → Dayline time/action → RETURN/feedback → worker or native source`

Dayline owns time/execution only. It does not own HOUSE state, Shopping lifecycle, COMMS source/coverage, CURRENT, Hermes, or Calendar.

## Mobile delta

- First phone visit opens the existing PLAIN action projection once. Saved projection is respected thereafter.
- ATLAS remains available with identical task identity/state.
- Utility/plain actions get a 38px minimum target; LIVE controls get 34px.
- CAPTURE + REALITY PORT remain visible on phone.
- Verbose FIELD fronts/gates collapse behind one FIELD control.
- CAPTURE inherits current Dayline contexts instead of hard-coding `computer`.
- COPY FEEDBACK becomes COPY HANDOFF while retaining the same explicit user-copy transport.
- Outbound schema advances to `atlas-dayline-feedback/v0.2` with `authority: NONE`, `trigger: USER_EXPLICIT_COPY`, live source pointers, and an explicit worker boundary.

## Hermes automation relation

The recovered Hermes automation model remains unchanged: cheap changing-input loops may wake bounded work, but Hermes is a replaceable executor and not canonical memory/scheduler. This patch gives such a worker a lawful evidence packet; it does not connect the public browser directly to a private runtime.

## FI relation

FI's route ontology lint remains diagnostic. Atlas Dayline is still the same PLAN object; only projection/use and an outbound evidence contract changed.

## Stop

No calendar OAuth/write, no fourth inbound reality adapter, no Dayline daemon, no hidden auto-import, and no RETURN→source mutation until mobile lived proof.
