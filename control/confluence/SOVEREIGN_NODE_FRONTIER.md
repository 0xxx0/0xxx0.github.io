# SOVEREIGN NODE / PROVIDER FRONTIER

Status: **PARKED / EXECUTABLE INFRASTRUCTURE DONOR**  
Updated: 2026-09-22  
Recipient surfaces: **CONFLUENCE agent roles / Router-Policy / MEDIA REFINERY / future local-cloud execution**

## Current reading

This packet does **not** become another active front.

`CURRENT` is conversion-first and explicitly says progress requires a usable capability, exact recovery that unlocks one, user-facing simplification, or a real-world/other-human consequence. Receipt-only coordination does not count.

The useful sovereign-node residue is therefore:

> **Own contracts, policy, receipts and canonical state; rent/replace models, media systems and agent runtimes.**

Canonical truth remains filesystem + Git + typed receipts + explicit RETURN.

Provider session memory, agent chat state and vendor job state remain non-canonical.

Runtime map: `/control/SOVEREIGN_NODE_RUNTIME.json`

## Transfer unit

Three typed boundaries remain sufficient:

```text
DecisionProvider
MediaProvider
AgentExecutor
```

They share:

```text
typed request
→ provider / model×harness adapter
→ typed result
→ policy gate where required
→ ExecutionReceipt
→ durable external evidence
```

Contracts:

- `/control/contracts/sovereign-node/receipt.ts`
- `/control/contracts/sovereign-node/provider.ts`
- `/control/contracts/sovereign-node/decision.ts`
- `/control/contracts/sovereign-node/media.ts`
- `/control/contracts/sovereign-node/agent.ts`
- `/control/contracts/sovereign-node/index.ts`

The contract layer contains no vendor imports.

## Hard laws

1. **PROVIDER STATE != CANONICAL STATE.**
2. **MODEL DECISION != EXECUTION AUTHORITY.**
3. **SECURITY / INVALID REQUEST / AUTHORITY DENIAL STOP.** Never bypass them through fallback.
4. **NO SILENT CLOUD FALLBACK.** Provider transitions must be visible in receipts.
5. **MODEL × HARNESS IS THE EXECUTOR UNIT.** Benchmark OpenClaw+model, Hermes+model, Grok Build+model, etc.; do not compare naked model names as if orchestration were neutral.
6. **DEGRADED LOCAL FLOOR SURVIVES.** Files, Git and deterministic LocalTaskRunner remain usable when every model/provider is unavailable.
7. **ONE BOUNDED MOVE → RETURN → REPLAN.** No successful executor run grants sequel authority.

## DecisionProvider

Candidate path:

```text
hosted decision service
→ local Ollama model
→ deterministic rules
→ REVIEW / ABSTAIN
```

A security/policy rejection terminates the chain rather than entering fallback.

Use this boundary for classification, routing, confidence gating and bounded semantic decisions. Do not use it to authorize destructive/external actions.

## AgentExecutor

Current unequal candidates:

- **OpenClaw** — replaceable executor/control-plane candidate; current proof target is 2026.9.5 or a later security-equivalent pinned build.
- **Hermes-Agent 0.21.3** — bounded supervisor/research/executor adapter; repo role remains read-only by default unless a task packet grants write authority.
- **Grok Build / current Grok coding model** — optional cloud comparison row only; its harness and model are evaluated together.
- **LocalTaskRunner** — deterministic degraded floor.

Current evidence requirement:

```text
same sacrificial workspace
+ same task corpus
+ same canonical files
+ external receipts
→ compare correctness / boundary fidelity / failure visibility / cost / latency
```

Do not let any executor become identity or memory authority for the project.

## MediaProvider — first real graft

MEDIA REFINERY is now the first concrete recipient of the provider law.

Current local split:

- `qwen3.5:9b` — bounded bulk multimodal semantic overlays through Ollama;
- `qwen3.5:27b` — optional local adjudicator/supervisor candidate, never auto-pulled by default;
- deterministic refinery — identity/hash/dedupe/contact-sheet/control-map/recipe compilation;
- Hermes — exception/supervision layer, not bulk-image worker;
- Higgsfield API/MCP — optional remote rendering provider only after explicit TRANSLATE/render authority.

Source media stays read-only. Remote rendering is downstream of reviewed recipes, never part of curation authority.

## Prepared executable proofs

### Baseline freeze

`/scripts/sovereign-node-freeze.sh`

Captures host/runtime versions, Nix metadata/generations, Ollama state, agent diagnostics and hashes provider state without copying secrets.

### Neutral executor fixture

`/scripts/sovereign-node-bakeoff-init.py`

Creates one sacrificial Git workspace and ten task fixtures. It launches no agent. Any executor may be tested against the exact same external state.

### Ollama endurance

`/scripts/sovereign-node-qwen-endurance.py`

Runs configurable native `/api/chat` endurance tests with JSONL timing/process receipts. Model and context are explicit runtime arguments; no tag or provider configuration is mutated.

## Drop-in prompts

- `/control/prompts/SOVEREIGN_NODE_SCOUT.md` — deep material-delta watch/research worker;
- `/control/prompts/SOVEREIGN_NODE_EXECUTOR.md` — replaceable bounded executor role;
- `/control/prompts/SOVEREIGN_NODE_CONFLUENCE_RETURN.md` — compress a run into durable transfer + re-entry conditions.\n- `/control/prompts/SOVEREIGN_NODE_REENTRY.md` — cold-start a successor from repo truth without replaying this chat.

These prompts are portable context packets, not personalities.

## Frontier Stack Watch role

The automated watch is a scout, not an execution queue.

Useful output:

```text
WHAT CHANGED
→ WHY IT MATTERS HERE
→ ADOPT / TEST / WATCH / IGNORE
→ smallest bounded test
→ PASS / FAIL / ROLLBACK
→ RETURN
```

Routine churn stays suppressed.

## Re-entry triggers

Reopen this packet only when one is true:

1. a material security/compatibility change affects an installed provider;
2. an active conversion needs a decision/media/agent capability;
3. OpenClaw/Hermes/Grok/local runner must be selected for a real operation;
4. canonical state is being trapped inside framework memory;
5. a local-vs-cloud or model×harness decision needs measured evidence;
6. a provider can materially simplify a current head without creating another control product.

## Stop conditions

Do not:

- put this packet into NOW because new AI tooling exists;
- build a universal router/UI/vector DB merely to coordinate providers;
- auto-pull heavyweight local models as setup side effects;
- treat agent memory as collaboration memory;
- treat fallback as permission expansion;
- publish/render/mutate source media from the curation loop;
- create receipt-only PR churn after the kit is executable.

## Confluence transfer

```text
FROM: Frontier Stack / sovereign-node research
TRANSFER: typed provider boundaries + external canonical state + explicit fallback + executable proofs
TO: Router/Policy / MEDIA REFINERY / future local-cloud execution
PRESERVE: source identity, external state, explicit authority, graceful degradation, RETURN
CHANGE: providers/models/harnesses may change freely
TEST: bounded fixture or active recipient operation
RETURN: measured evidence or explicit block
```

## RETURN

This packet succeeds when a future worker can cold-start from the repo, replace an intelligence provider without semantic migration, run one bounded proof, lose the provider completely, and reconstruct state from files/Git/receipts.

If no active conversion is unlocked after that, **PARK**.
