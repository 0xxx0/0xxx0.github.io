# ONE RETURN / NINE GATE — LIVE LINEAGE LANE

**Status:** playable import-reified lane  
**Route:** `/sleeper/one-return/`  
**Parent:** Sleeper lineage; not the current Sleeper POV head and not a CONFLUENCE-owned application.

## Recovered object

The latest actual maze-game artifact recovered in File Library is `one_return_nine_gate_rebuilt_v0.1.html` (2026-09-13). It explicitly separates itself from Semantic City and restores the attested ONE RETURN shell:

```
SOURCE / SEED
→ deterministic ASCII labyrinth
→ semantic letters in walls
→ Nine ordered Gates
→ challenge
→ topology deformation
→ CITY / DISC / TRACE
→ RETURN
→ previous-run ghost
```

The historical standalone source remains lost. The public lane therefore preserves the latest recovered runnable mechanics and code shape without claiming byte identity.

## Distinguish

- **ASCII Sleeper POV 0.8** — current constraint/evidence head.
- **ONE RETURN / NINE GATE** — playable maze ancestor lane; traversal is intrinsic.
- **Workfield v1.2** — practical source/task substrate; walking was deliberately demoted there.
- **Grid Path Compiler** — later path/route/compiler instrument; related donor, not the maze-game head.
- **CONFLUENCE** — may transfer mechanisms out of this lane, but does not own or absorb the game.

## Why restore it

The maze is not merely an old UI. It contains a different operation class that current Workfield/AXIAL/Lens do not reproduce:

> semantic success changes navigable topology, and the previous traversal returns as a spatial ghost.

That deserves a live comparison surface rather than a paragraph in lineage notes.

## Transfer candidates

Only after use:
- gate order / authored Nine-Gate or cell-path import;
- ghost-path comparison as evidence;
- source→topology transformations;
- DISC projection as state-equivalent but cost-hiding reading;
- route witness as a compact RETURN signature.

No transfer is implied merely by reactivation.


## Implemented receipt seam

Grid Path V4 emits a `BY/GRID-PATH-2` RETURN containing:
- source fingerprint / pack / matrix;
- stable path id;
- addressed cells + typed edges;
- compressed path address;
- flat + stranded reading.

ONE RETURN now accepts that receipt directly.

The transfer is deliberately lossy and named:
```
BY/GRID-PATH-2
  preserve: source fingerprint + path id + address + reading
  hide: rectangular geometry + typed BY edges as maze geometry
→ source units / deterministic seed
→ new ONE RETURN maze projection
→ RETURN with upstream provenance
```

This makes the maze a lawful new projection of the compiler receipt, not a replay of compiler geometry.
