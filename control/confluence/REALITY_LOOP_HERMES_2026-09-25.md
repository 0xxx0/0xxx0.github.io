# REALITY LOOP / HERMES ATTACK PACKET — 2026-09-25

Authority: NONE. Executor packet only. Read `/AGENTS.md` and `/llms.txt` first; CURRENT remains attention authority.

## PURPOSE

Reduce the distance between a large collaboration and reality actually changing.

Do not “unify the apps.” Compose their unequal authorities:

- FIELD / CURRENT: what deserves attention.
- HOUSE / SPATIAL: addressed physical substrate; HA/HOUSEBUS alone owns device truth/actuation.
- SHOPPING FIELD: capability gap → market evidence → lifecycle; money/seller action is human-only.
- HUMAN PORT / COMMS: exact source thread + addressed obligations; no send authority.
- ATLAS DAYLINE: time/context/execution surface for a small day.
- RETURN: observed evidence after execution.
- Hermes: bounded executor/researcher; never a second scheduler or state authority.

Human status: Atlas Dayline LIVE has been opened in ordinary use. Structured lived feedback is not yet returned.

## CURRENT IMPLEMENTATION HEAD

Atlas Dayline LIVE 0.2 / REALITY PORTS uses a deliberately tiny transport:

`atlas-dayline-handoff/v0.1`
sessionStorage key: `atlas.dayline.handoff.v01`

This is a one-envelope explicit handoff, not a queue, bus, database or synchronization layer.

Implemented directions:

1. HOUSE / SPATIAL selected address → Dayline CONTEXT.
   - preserves room id / selected projection / room mode.
   - selected address != physical presence.
   - explicit APPLY CONTEXT in Dayline.

2. SHOPPING selected item → Dayline TASK.
   - prefers `next_action`; bounded fallback only.
   - preserves item id/state/freshness/gate/adoption criterion.
   - no purchase/state promotion.
   - explicit ADD TO DAY.

3. COMMS one OPEN signal → Dayline TASK.
   - HUMAN OPEN preferred; otherwise DERIVED stays labeled DERIVED.
   - preserves source SHA + message/range + kind/origin/text.
   - does not copy whole conversation.
   - no send/coverage mutation.
   - explicit ADD TO DAY.

All retain `return_to` and source provenance.

## THREE ACTIVE ATTACKS MAX

### A / LIVED HANDOFF PROOF — FIRST

Run/repair representative browser tests for all three handoffs, especially Android/mobile/PWA navigation:

`source → explicit handoff → Dayline displays pending envelope → APPLY/ADD → source identity preserved → BACK works`.

Check sessionStorage survival across same-tab navigation, PWA cache behavior, tiny-screen controls and failure/clear behavior.

Do not fake lived user evidence. Automated/browser evidence may prove plumbing only.

### B / RETURN-TO-SOURCE PROTOCOL — DESIGN + ONE PROOF ONLY

After A is green, implement one smallest reversible proof of:

`Dayline focused task + RETURN → source-addressed receipt OFFER → explicit ACCEPT in native source`.

Choose the source with the cleanest native receipt semantics. Do not implement three at once.

Strong candidate: SHOPPING, because a Dayline return can attach observed task evidence while Shopping alone decides lifecycle transition.

Requirements:

- exact sourceRef survives;
- source tool owns acceptance;
- accepting a receipt does not automatically mark BOUGHT/RECEIVED/TESTED/ADOPTED;
- rejection/ignore is lossless;
- no durable universal event bus.

### C / REAL-WORLD TIME — CALENDAR ANCHOR READ PROOF

Only after A is usable and without stealing B's active slot if B is underway:

`calendar event identity → read-only immutable Dayline anchor`.

No autonomous rescheduling. No calendar write until separate preview/commit/receipt proof exists.

## HOUSE / COMMS / SHOPPING CONVERGENCE QUESTIONS

Use these as falsification questions, not feature prompts:

- HOUSE: can an addressed room/context materially change what Dayline surfaces without pretending occupancy?
- SHOPPING: can an acquisition task re-enter the day at the correct lifecycle stage without laundering FOUND→OWNED?
- COMMS: can an obligation enter time while exact source address remains recoverable and derived cues stay advisory?
- RETURN: can completion evidence flow back to the owning system without Dayline becoming the owner of its state?
- SILENCE: when no source item deserves today, does the system show nothing rather than manufacture work?

## FORBIDDEN

- no new planner core;
- no universal session/event bus;
- no copied CURRENT/WAITING authority;
- no automatic import of local private state;
- no automatic Home Assistant actuation;
- no purchase or seller messaging;
- no automatic communication send;
- no Dayline-driven lifecycle promotion in Shopping/Comms/House;
- no “AI decides the day” loop;
- no giant dashboard joining all domain data;
- no mass archaeology unless a live seam needs one exact donor.

## BOARD TELEMETRY WARNING

`/nexus/board.html` still emits Hermes branch rows whose Latest value is `fatal: ambiguous argument ...`. Do not infer branch health from those rows. Repair generator/ref resolution only if it blocks actual coordination; otherwise use Git refs/PRs/receipts as ground truth.

## REQUIRED RETURN

For each bounded attack:

`SOURCE → DELTA → FILES → TEST → OBSERVED RESULT → UNPROVED → RESIDUE → ONE NEXT`

Create no successor task automatically. RETURN to CURRENT/confluence for replanning.

## STOP CONDITION

We have won this phase when a real-world obligation from HOUSE, SHOPPING or COMMS can enter today's Dayline with source identity intact, be acted on, and return evidence to its native owner without any shared authority or hidden synchronization layer.


## CONTINUATION — DAYLINE / CONFLUENCE WORKFIELD

A new bounded candidate exists at `/dayline/`.

It is not a new planner. It operates on:
- the existing Atlas Dayline localStorage DayState;
- the existing `atlas-dayline-handoff/v0.1` session envelope;
- read-only CURRENT + manifest;
- `poly-atlas-return-f v3` with carrier `dayline-confluence/v0.1`.

Existing HOUSE / SHOPPING / COMMS handoffs now target this candidate for the lived proof. Atlas remains optional depth.

### Hermes priority

Do not improve the candidate speculatively.

If the user supplies a `dayline-confluence/v0.1` RETURN or explicit lived friction:

1. recover current master + `/control/confluence/DAYLINE_CONFLUENCE_WORKFIELD_2026-09-25.md`;
2. identify exactly one failed continuity invariant;
3. make at most one bounded correction;
4. verify;
5. RETURN and stop.

Valid failed invariants include:
- page cannot scroll/reach actions on the actual phone;
- source identity is lost;
- more than three primary moves become necessary;
- witness cannot attach to the held Dayline object;
- RETURN cannot recover the native source;
- a source authority is silently mutated.

Do not respond to the new candidate by:
- creating another shell;
- adding more source tabs;
- embedding native apps;
- inventing another handoff schema;
- making Hermes the scheduler.

Promotion remains a human-use judgment.
