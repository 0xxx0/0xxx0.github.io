# DESIGN ENGINEERING DONOR — SEPTABEE / INTERFACE STUDIES / TANGIBLE MEDIA
Date: 2026-09-24
Status: ACTIVE DONOR / CROSS-THREAD DESIGN LAW
Authority: donor packet only; it does not replace domain truth, source identity, or current runtime heads.

## Why this exists

The user supplied:
- Septabee tutorial/video: https://www.youtube.com/watch?v=lW8Z98pXuSE
- Interface Studies channel: https://www.youtube.com/@interfacestudies

These are not to be copied as skins. They clarify a design-engineering direction already latent in FIELD / Fold-Bloom / AXIAL / Pathweaver / radial work:

> **interface form should expose causal structure, and interaction grammar should be generated by the thing being manipulated rather than pasted on top of it.**

This packet is durable shared coordination. New threads should recover it before inventing another UI aesthetic branch.

## OBSERVED / RECOVERED DONORS

### Septabee / Lost Robot
Corroborating current public coverage:
- https://cdm.link/septabee-is-here/
- https://newsnix.com/articles/septabee-lost-robot-daw-dataflow/
- https://weraveyou.com/2026/09/septabee-free-daw-lost-robot-nine-rewrites-b-plus-plus/

Useful mechanisms:
- spatial / node-like dataflow rather than a compulsory track-list hierarchy;
- operators can live on connections, so the edge is computational matter rather than decoration;
- nested / infinite zoom became a core navigation idea in its lineage;
- patterns and tuning are first-class patchable material;
- the visual grammar and the audio computation are coupled closely enough that the UI feels like an instrument/world rather than an admin surface;
- idiosyncratic motifs create memorable topology, but the transferable part is **structural distinctness**, not pomegranates or neon.

Boundary:
- do not copy Septabee's aesthetic, terminology, mascots, or unconventionality for its own sake;
- use it as evidence that a deep technical model can generate an equally deep spatial interface.

### Interface Studies / Saleh Kayyali
Index: https://www.mskayyali.com/interface-studies/
Channel: https://www.youtube.com/@interfacestudies
Current writing: https://interfacestudies.substack.com/

Particularly aligned topics already indexed there:
- radial / pie menus;
- zoomable user interfaces;
- modes and depth from fewer inputs;
- responsive surfaces;
- verbs vs nouns;
- software as place;
- the receding user / designing for absence;
- horizontal vs vertical navigation;
- undo / back / return distinctions;
- settings overload;
- interfaces flattening into prompts;
- agents vs operating systems;
- intuitive interaction as a historical / learned relation rather than magic.

Transfer:
- treat interface conventions as contingent mechanisms with history, not defaults;
- ask what action model each control teaches;
- prefer fewer generative inputs whose meaning changes lawfully with context over wide flat banks of unrelated controls;
- preserve spatial memory and re-entry;
- design absence / autopilot / peripheral witness explicitly, not as inactivity;
- distinguish BACK, UNDO, RETURN, PARENT, REPLAY and COMMIT.

### MIT Media Lab / Tangible Media
Anchors:
- Tangible Bits: https://www.media.mit.edu/publications/tangible-bits-towards-seamless-interfaces-between-people-bits-and-atoms-2/
- Radical Atoms: https://tangible.media.mit.edu/project/radical-atoms/
- Hiroshi Ishii overview: https://www.media.mit.edu/people/ishii/overview/

Transfer:
- foreground manipulation + peripheral / ambient witness;
- interactive surfaces rather than UI chrome;
- bind digital state to graspable / spatially legible form;
- bidirectional synchronization: representation should both display state and accept meaningful transformation;
- quality bar = artistic + analytic, poetic + pragmatic;
- physical / sensory skills are computational resources, not decoration.

## DESIGN LAW

### 1. SUBSTANCE GENERATES FORM
Do not begin with cards, settings pages, dashboards or hero typography.

Begin with:

```
OBJECT
→ ADDRESS
→ RELATIONS
→ OPERATORS
→ CONSEQUENCE
→ RETURN
```

Then derive geometry, controls, typography and animation from those relations.

### 2. EDGE IS A FIRST-CLASS PLACE
A relation may carry:
- transform;
- gain;
- uncertainty;
- timing;
- constraint;
- provenance;
- seam;
- permission.

Do not force every operation into a node/card/sidebar if its natural address is the connection.

### 3. FEWER INPUTS, DEEPER CONTEXT
Prefer one gesture / dial / path whose meaning is constrained and inspectable over many unrelated controls.

Examples already in our lineage:
- radial selection;
- fold / bloom polarity;
- path traversal;
- aperture scale;
- source-time scrub;
- drag-to-steer + branch choice;
- pressure/speed/tilt as brush state.

### 4. SPATIAL MEMORY IS FUNCTION
A user should be able to remember **where** something lives.

Stable relative position is evidence. Re-layout should be deliberate.

### 5. UI MAY RECEDE
When the source/world is legible, chrome should disappear.

Use:
- peripheral witnesses;
- contextual apertures;
- temporary toolglass;
- local labels;
- event-driven surfacing.

Do not make every capability permanently visible.

### 6. TYPOGRAPHY IS SIGNAL, NOT ARCHITECTURE
Large editorial typography is valid for a doorway, manifesto, or deliberate pacing event.

It is not a substitute for operational structure.

Inside instruments:
- type should indicate state, address, scale, action or consequence;
- giant headings should not consume the work surface;
- forms should feel like instruments / consoles / sheets / fields, not marketing documents.

### 7. PRIMITIVE-FIRST IMPLEMENTATION
Prefer:
- HTML;
- Canvas / SVG;
- CSS;
- Web Audio;
- small ES modules;
- deterministic local state;
- explicit JSON packets;
- browser-native pointer / keyboard APIs.

Add dependencies only when they buy a hard missing capability.

Reason:
- keeps causal model inspectable;
- permits code-golf / demoscene-like density;
- makes composition and reuse cheap;
- reduces framework-shaped aesthetics;
- preserves local/offline control.

### 8. AESTHETIC VECTOR ≠ HOUSE STYLE
Nous/Hermes, Septabee, MIT Media Lab, demoscene, thi.ng, Lindong, Xem, print/editorial, sumi/ink, square-kufic, games and physical instruments are **donors**, not one mandatory skin.

HOUSE code should instead preserve a small set of formal invariants:
- hard geometry;
- exact alignment;
- dense but quiet state;
- purposeful motion;
- explicit hierarchy;
- low decorative radius;
- meaningful spatial persistence;
- material / causal feedback;
- typography subordinate to operation on working surfaces.

### 9. DELIGHT MUST HAVE A CAUSE
A surprising visual, sound, motion, haptic or transformation should reveal:
- an event;
- a relation;
- a successful operation;
- a scale transition;
- a hidden structure;
- a recovered state.

Delight without a cause becomes wallpaper.

### 10. QUALITY BAR
Target research-demo quality where:
- the mechanism is novel/useful enough to deserve explanation;
- the interaction can be learned by touching it;
- the visual form makes the mechanism more legible;
- implementation is small enough to inspect;
- failure / uncertainty remains visible;
- the artifact produces a durable RETURN.

“MIT Media Lab or better” means rigor of concept + prototype + presentation + evaluation, not imitation of institutional aesthetics.

## APPLICATION PRIORITY

### FOLD//BLOOM
Immediate:
1. reusable wet-media / brush engine;
2. explicit experience profile with few macro controls and optional deep tuning;
3. shareable zero-file demo URLs;
4. drop / anticipation choreography driven by source evidence;
5. source/event/path continuity between RIDE / PULSE / READ / LOCI / INK / DATA;
6. edge-operators on event tape / paths where transformations naturally belong.

### FIELD INDEX
For forms / operational surfaces:
- migrate away from generic web-form appearance;
- make source/address/state legible first;
- compact labels into instrument rails;
- use spatial groupings that persist;
- reveal advanced controls through aperture, not permanent form sprawl;
- retain accessibility and native input semantics.

Do not redesign the entire index in one pass. Create one shared primitive and migrate high-use forms as bounded transforms.

### HOUSE / PHYSICAL
Treat physical interfaces as the natural extension:
- light / sound / airflow / motion as peripheral state;
- compression rails / partitions / rotary surfaces as graspable address spaces;
- sensors and actuators should publish the same source→state→operation→return semantics.

## CURRENT IMPLEMENTATION GATE

A proposed design change should answer all seven:

1. What exact object is manipulated?
2. What address remains stable?
3. What relation does the geometry expose?
4. What operation is actually available?
5. What sensory change witnesses consequence?
6. What is source evidence vs profile/preference?
7. How does the user RETURN / undo / replay / inspect?

If those are unanswered, styling work is premature.


---

# FIELD LAB FOLLOW-UP — READ / LOCI
Date: 2026-09-24
Status: ACTIVE FOLLOW-UP NOTES / NO RUNTIME MUTATION YET
Source: direct user critique after FIELD LAB 0.1 use.

## OBSERVED

### READ
The current LAB READ projection violates the instrument law in this same document:

- source text lives in a persistent textarea occupying scarce work surface;
- the central hero-size "READ" label remains in the operative field after mode identity is already known;
- controls + explanatory text compete with the temporal reading object;
- the user is asked to look at interface architecture rather than read.

This is not merely aesthetic dislike. It is an operational failure: chrome displaces the object.

### LOCI
The current LAB LOCI implementation is promising but intentionally toy-sized:

- arbitrary tokenization;
- hard \`slice(0,12)\`;
- one generated radial/wobble layout;
- one fixed sequential hit order;
- no source hierarchy;
- no imported path law;
- no large-source strategy;
- no course authoring;
- no durable route/recall receipt.

The recovered Pathweaver / Verse lineage already contains stronger laws:
\`CELL → PATH → ADDRESS → VIEW → RETURN\`, explicit path programs, ROWS / COLUMNS / BOUSTROPHEDON / CUSTOM traversal, stable cells, captures, typed BY transitions, route glyph projection and reversible source/path state.

Therefore LOCI should consume those mechanisms instead of inventing a second mnemonic path engine.

## READ — NEXT DESIGN

### R1 · SOURCE INTAKE MUST RECEDE
Treat paste/import as an aperture, not a permanent editor.

Candidate interaction:
1. empty READ state exposes one small PASTE / DROP / USE CURRENT SOURCE aperture;
2. after binding, source intake collapses to a thin source rail/witness;
3. direct reading surface owns the center;
4. source editor reappears only on explicit SOURCE action.

No hero title remains in the center after the first interaction.

### R2 · CENTER = READING OBJECT
The center should contain only material that participates in reading:
- current word / phrase;
- minimal near context when useful;
- ORP/fixation witness if enabled;
- ghost/user trajectory or path residue;
- source-position witness.

Mode labels move to the perimeter or disappear.

### R3 · CONTROLS RECEDE DURING RUN
START should transform the surface:
- controls collapse;
- pointer/touch/keyboard can reveal a temporary toolglass;
- progress/ghost remain peripheral;
- pause/regress should be spatially stable and reachable without opening a settings panel.

### R4 · DO NOT FORK READFIELD
LAB READ is a training/game projection over canonical text address.
READFIELD remains the serious reader and source-position authority.

Candidate shared state:
\`source_id + canonical text address + aperture + pace + optional ghost trajectory\`.

LAB-specific race state remains projection/training evidence, not canonical source truth.

### R5 · RACE SHOULD BECOME TRAJECTORY, NOT SCOREBOARD
Instead of YOU/GHOST/Δ as dashboard boxes, render two traces through the same addressed source.
Useful events:
- catch;
- fall-behind;
- deliberate regress;
- pause;
- recovery;
- finish.

This can later join track/course grammar without converting reading into a fake speed score.

## LOCI — NEXT DESIGN

### L1 · REFRAME AS COURSE / PATH PROJECTION
LOCI is not fundamentally "tokens around a circle."

It is:

\`\`\`
SOURCE
→ ADDRESSABLE UNITS
→ ROUTE / COURSE
→ LOCUS PROJECTION
→ TRAVERSE / RECALL
→ RETURN
\`\`\`

The same course grammar can support:
- mnemonic loci;
- Pathweaver reading paths;
- track / rhythm course design;
- READ race trajectories;
- verse/cell paths;
- event-tape checkpoints;
- later spatial/ROOM projection.

Do not claim those projections have equal semantics. They share route/address mechanics.

### L2 · LARGE SOURCE = HIERARCHY, NOT MORE DOTS
For large pasted text, derive multiple address scales rather than truncating:
- source;
- section / paragraph;
- sentence / phrase;
- token only when aperture requires it.

A long source should first become a course of meaningful chunks/landmarks. Zoom/aperture can expose finer units locally.

Candidate deterministic policy:
- <= 16 units: direct loci;
- larger source: coarse chunk course first;
- expand one locus into sub-loci on demand;
- preserve exact source address at every level.

These thresholds are experiment parameters, not canon.

### L3 · ACCEPT A SMALL ADAPTER SET
Do not invent a universal schema yet. Normalize a few existing input forms into an internal candidate course object.

Initial adapters:

1. **PLAIN TEXT**
   - preserve exact source string/hash;
   - derive paragraph/sentence/phrase addresses;
   - layout/path is derived evidence.

2. **PATHWEAVER CAPTURE / PATH PROGRAM**
   - consume stable cells, path order, traversal law and custom path where recoverable;
   - imported authored route remains authored route;
   - LOCI adds mnemonic projection/training only.

3. **FOLD//BLOOM EVENT TAPE**
   - BEAT / PHRASE / SECTION / OPERATION events become time-addressed checkpoints;
   - measured timing remains evidence;
   - authored OPERATION remains authored;
   - enables memory/rhythm/course experiments without changing source truth.

Possible later adapter only after need:
4. **GENERIC ADDRESSED LIST / GRAPH**
   - explicit \`id + label + source_address\`, optional typed edges.
   - avoid accepting arbitrary JSON as meaningful structure merely because it parses.

### L4 · INTERNAL COURSE OBJECT — CANDIDATE, NOT NEW CORE
A local LAB-normalized object may look like:

\`\`\`json
{
  "source": {"id":"…","type":"text|path|event-tape"},
  "nodes":[
    {"id":"n0","address":"…","label":"…","cue":null}
  ],
  "edges":[
    {"from":"n0","to":"n1","by":"ORDER|GEOMETRY|CHOICE|EVENT"}
  ],
  "route":["n0","n1"],
  "projection":{"layout":"radial|grid|path|track","seed":"…"},
  "training":{"phase":"study|guided|recall|reverse"}
}
\`\`\`

This must stay an adapter/view object until at least two real consumers prove a shared helper removes duplicate machinery.

### L5 · COURSE AUTHORING
Recovered Pathweaver mechanisms should enter LOCI as optional course-authoring operations:
- ROWS;
- COLUMNS;
- BOUSTROPHEDON;
- CUSTOM draw/trace;
- later only if justified: spiral / ring / imported typed path.

For mnemonic use, allow node repositioning or route drawing as human authorship.
Deterministic auto-layout must be marked DERIVED.

### L6 · TRAINING PHASES
A better LOCI loop:

1. **STUDY** — labels/cues visible; traverse route.
2. **GUIDED** — current/next locus visible.
3. **RECALL** — labels hidden; recover path/content.
4. **REVERSE** — traverse backward.
5. **REPAIR** — expose missed locus + neighboring context.
6. **RETURN** — exact source + route + misses/hints/latencies retained.

Evidence vector rather than one score:
- order accuracy;
- source-position recovery;
- latency;
- hints;
- reversals;
- retained recall after delay.

### L7 · LANDMARK DISTINCTNESS IS A REAL VARIABLE
Method-of-loci depends on distinct places/cues; generic identical dots are a weak substrate.

Candidate non-AI landmark channels:
- deterministic source glyph;
- route geometry;
- node shape;
- short text fragment;
- source-derived audio/event witness;
- user-authored mark/INK thumbnail.

Test landmark channels against a plain-label baseline before adding generative imagery.

### L8 · TRACK DESIGN CONNECTION
The high-value convergence is a shared **COURSE** idea, not a shared visual skin.

A course can have:
- exact addressed checkpoints;
- typed edges;
- optional timing;
- authored branches;
- difficulty/constraint;
- sensory witnesses;
- traversal residue;
- RETURN.

Then:
- LOCI projects course as memory space;
- PULSE projects it as rhythm checkpoints;
- READ projects it as temporal text trajectory;
- Pathweaver projects it as cell route;
- Beat/event tape projects it as timed course;
- GLYPH compresses the route;
- future RIDE may embody it.

This is a candidate donor relationship. Do not promote COURSE to core until two existing hosts independently need the same structure.

## FIRST BOUNDED PROOFS

### Proof A — READ de-chrome
Change no semantics.
Remove persistent source textarea and central hero word from the active run state; collapse controls after START.
PASS if reading area materially increases and all source/edit/regress/pause/return operations remain reachable.

### Proof B — LOCI large text
Use one genuinely long pasted source.
Normalize paragraph/sentence chunks, create >12 stable addressed loci across a hierarchical course, traverse, hide, recall, RETURN to exact text addresses.
PASS if no source content is silently discarded and the user can recover source position from a locus.

### Proof C — Pathweaver import
Import one recovered Pathweaver path/capture.
Preserve exact cell IDs/order/law.
Project it as LOCI without claiming mnemonic meaning came from Pathweaver.
PASS if round-trip returns the same source/path authority.

### Proof D — Event Tape course
Import one event tape.
Project SECTION/PHRASE/selected BEAT/OPERATION events as checkpoints.
PASS if measured vs authored event classes remain distinguishable through export/RETURN.

## HOLD

- AI-generated palace imagery before landmark need is proved;
- universal course schema;
- arbitrary graph ontology;
- one scalar "memory score";
- full 3D world;
- automatic semantic chunking presented as truth;
- more typography/chrome;
- a separate LOCI app before LAB proves the interaction.

## ONE NEXT

Implement **Proof A + Proof B only** in one bounded branch:
READ recedes; LOCI stops throwing away large inputs.
Do not generalize COURSE until those two direct-use deltas are felt and verified.
