# XD / TRANSDUCTIONS

Status: experimental composition registry  
Current machine source: `/control/TRANSDUCTIONS.json`

## Why this exists

One place for speculative and active flows that are really the same operation under different carriers.

Do not create a new pipeline because the carrier changed.

```
SOURCE
  → FOCUS
  → TRANSFORM
  → COMMIT
  → OBSERVABLE
  → EVIDENCE
  → RETURN / NEXT
```

The implementation may be UI, conversation, an agent run, HAOS event handling, physical actuation, a document transform, or a game mechanic. Carrier is not identity.

## Standing desire — operator-confirmed 2026-09-25

Status: **DIRECTION REMINDER, NOT A LAW.** It adds no authority and overrides no evidence, identity, host, or human gate. It biases defaults for every future thread, worker, host, and pass.

The operator's stated desire: use more of the universal interface; keep absorbing (gameloop / lab / read / listen) into the shared carrier instead of adding surfaces; *"crush down all these surfaces and make dealing with this infinity possible."*

In practice, when proposing work:

1. **FOLD FIRST.** Ask whether the thing can fold losslessly into an existing head (see *Lossless artifact folding* below) rather than become a new sovereign surface. One operational sovereignty, many preserved evidence objects.
2. **ROUTE THROUGH THE CARRIER.** Where a mechanism migrates, express it as correspondence over the existing carrier grammar — F.L.O.W.E.R. / FU / Universal Interface Wheel → six-sector carrier, INTERPHASE hosts, Instrument Cell, `/control/TRANSDUCTIONS.json` — before inventing a bespoke pipeline. Carrier is not identity; hosts keep domain truth.
3. **GAMELOOP IS A TRANSDUCTION.** Play/loop mechanics (event tape → operator → consequence → RETURN) and the LAB projections (RIDE / PULSE / READ / LOCI / INK / DATA) belong to one family; absorb new asks there first.
4. **NO BREAKING CHANGE.** Compose before you create; fold before you fork; reuse before you rebuild.

Origin: operator message 2026-09-25 (via Hermes), landed on `hermes/standing-desire-20260925` from master `6589f6ba`.

## Exactly recovered donor

The frozen 2020 INTERPHASE prototype at:

`/recovery/poly-furnisher/interphase-lineage/interphase-2020.html`

contains:

- `flow / wolf` function composition
- Lens / Iso / Traversal focus
- Store with `action(transform)(event)`
- reducers
- transducer operations including `xduce`, `mapping`, `filtering`

This is donor evidence, not proof that the historical artifact implemented the current FIELD semantics.

Older shorthand: **xd**.

## Reconstructed continuity

Later forms repeatedly preserve the same deeper move:

- FTTC: Frame → Transform → Test → Commit
- SOURCE → TRANSFORM → RETURN
- HOUSE: OBSERVE → TARGET → ROUTES → TEST → RESULT → RETURN
- Scale Lens: stable object identity under changing lawful projection
- current bounded execution: one state delta → verify → RETURN → re-orient

These should compose through one registry rather than become competing workflow products.

## Current primitive

A transduction is recorded as:

| field | job |
|---|---|
| source | what arrives |
| focus | what part/address is selected |
| transform | bounded change |
| commit | where changed state can become durable |
| observable | what can actually be noticed/measured |
| evidence | why the change is believed |
| authority | maximum permitted consequence |
| reversibility | undo/revert/compensation/none |

Composition never raises the weakest member's authority ceiling.

## Lossless artifact folding

The active surface may shrink without deleting history.

A **lossless fold** means that an old surface can stop being a sovereign head when its useful state can round-trip through:

```
SOURCE ID
  → ADDRESS
  → APERTURE
  → PROJECTION
  → OPERATOR + PARAMETERS
  → AUTHORITY / COMMIT
  → RETURN
```

The old file, commit, image, receipt or physical study remains evidence. What disappears is duplicated **operational sovereignty**, not provenance.

`INTERPHASE 0.2` now implements the shared **instrument cell** as a host protocol rather than a new sovereign app. FIELD, READFIELD, Scale Lens, LISTEN and a generic DOM/browser-extension host expose unequal domain truth through the same selection/multifocus/projection/operation/RETURN machinery. The broader projection inventory remains LINE, RING, SPIRAL, GRID, FOLD, ROOM, RSVP and GLYPH; the implemented browser subset additionally exercises PAGE, FOVEA, FLOWER-KEY and TABLE. A host supplies the domain vocabulary; the geometry does not get to invent one.

### Six-sector carrier

A center + six sectors/petals is retained as a useful carrier candidate because the recovered corpus repeatedly used six-way flower/cube/wheel forms and foldable radial material studies. It is deliberately **not** promoted to a six-part ontology.

- center = host-defined invariant / intent / exact focus
- sectors = six host-defined facets, inputs or operators
- rotation/radius/fold only acquire meaning through explicit mappings
- visible operation and commit state must remain legible
- F.L.O.W.E.R. / FU semantics are presets, not compulsory labels
- the exact remembered key-shaped six-panel historical artifact is still UNKNOWN until directly recovered

### Current implementation

- core: `/lib/interphase-core.js`
- generic DOM adapter: `/lib/interphase-dom.js`
- host adapters: FIELD / READFIELD / Scale Lens / LISTEN
- load-unpacked browser extension: `/manifest.json` + `/interphase-extension.js`
- proof donor: `/spikes/003-one-instrument/`
- recovered explicit residue: **CONTENT / AUTHORITY / DEPTH / TIME**
- six-office carrier preset: **SOURCE / FRAME / FOCUS / OPERATE / WITNESS / RETURN**
- PR #184 merged at `b4324536…`; full public-surface-check PR run 35815194125 passed.

The six offices remain a preset, not ontology. FLOWER-KEY and ROOM reuse the same panel identities; changing geometry does not recreate the panels or move host authority.

### Current fold map

- APERTURE + AXIAL + Scale Lens + READFIELD FOCUS + LISTEN ADDRESS/APERTURE → one **address/aperture/lens instrument** with unequal hosts.
- DataDisc/radial + strip + spiral + fan/fold + ROOM + RSVP + glyph → one **projection inventory**.
- F.L.O.W.E.R. + FU + Universal Interface Wheel + fold-build-transform studies → one **six-sector carrier grammar** plus named semantic presets.

Do not delete or hide a donor until a round-trip fixture proves source identity, address, aperture, parameters, operator ordering, authority and RETURN survive. If a unique behavior cannot be reconstructed, it has not been folded. Implementation proof authorizes adapter migration, not bulk deletion; lived-use residue still decides demotion.

## Active slice: passive refinement

```
use
 → signal
 → candidate preference
 → low-risk default
 → use
 → accept / override / backtrack
 → revised candidate
```

Current branch implementation:

- UI signal metadata stays local.
- Source/payload text is never stored in preference history.
- A FIELD map-mode default requires at least 3 observed choices and ≥0.60 top share.
- HUMAN PORT proposes one likely route.
- Choosing another route is stronger evidence than accepting the candidate.
- No observed preference grants sending, spending, deletion, publication, or physical actuation authority.

## Proposed composition: bounded autonomous work

Not a software factory.

```
CURRENT HEAD
 → find smallest named delta
 → execute
 → verify
 → RETURN
 → re-orient
```

The unit being built is not “software”; it is a verified state transition. Code is only one possible residue.

A recursive run may continue without a human only when a bounded recurrence contract already authorizes the next class of move. Otherwise one consequential delta closes the run.

## Placeholder: HOUSE / HAOS

```
event
 → normalize
 → state estimate
 → candidate transform
 → authority gate
 → act
 → measure
 → RETURN
```

Sensing and actuation remain separate transductions. More sensing never implies more authority.

Prefer purpose-level events (motion, occupancy, presence, task completion, device transitions) over leaking entity implementation details into FIELD.

## Promotion rule

A speculative composition remains here until it demonstrates one of:

1. repeated reuse across unequal carriers;
2. lower decision/re-entry cost;
3. a clearer evidence/authority boundary;
4. a measurable closed feedback loop.

Then promote the mechanism, not the prose.

## Stop rules

- no new orchestration product for a renamed chain;
- no inference promoted from one observation;
- no recursion without delta or new evidence;
- no pipeline whose output cannot identify its evidence and authority;
- no duplicate world-model beside canonical FIELD/HOUSE state;
- no marketing layer around a transduction.

## Current experiment

Branch: `field/passive-feedback-loop-20260921`

The experiment is intentionally small: prove that passive evidence can alter a reversible presentation default and correct a route candidate without asking the user to configure a preference model.
