# ATLAS DAYLINE / MOBILE REALITY RETURN — 2026-09-25

## Trigger

Direct lived evidence from the user: **Dayline is not functionally viewable/useful on mobile.**

This overrides the earlier source-only “responsive” pass. PR #254 proved lawful FIELD I/O, not phone usability.

## Recovered automation handoff

The separate Hermes work converges on one rule: Hermes is a replaceable executor, not canonical memory/authority. Cheap automation may watch changing inputs, but should emit only meaningful change and remain silent otherwise.

Therefore Dayline does **not** gain an agent/runtime/backend.

The lawful seam is:

`REALITY → local DayState → explicit COPY HANDOFF → /llms.txt + live authority → one bounded worker move → RETURN`.

## Delta

1. First mobile visit defaults once to **PLAIN**, the existing action/list ablation. Saved user projection wins thereafter.
2. The full ATLAS remains one tap away and retains the same task identities.
3. Primary phone actions receive a larger touch floor.
4. LIVE keeps the reality-capture line visible but collapses verbose FIELD candidate/gate detail on phone.
5. Quick capture inherits the current Dayline contexts instead of falsely hard-coding `computer`.
6. COPY FEEDBACK becomes **COPY HANDOFF** and emits `atlas-dayline-hermes-handoff/v0.1` with authority `NONE`, trigger `USER_EXPLICIT_COPY`, live source pointers, local Dayline evidence, residue, one-next and a stop boundary.

## Important boundary

This does **not** directly automate Hermes, Calendar, GitHub, messaging, devices or accounts. A browser page cannot lawfully pretend that an executor is connected.

The packet is deliberately ready for a local Hermes/ChatGPT/Codex consumer. A future private adapter may ingest it only after that adapter exists and its policy/effect boundary is explicit.

## FI relation

The new FI ontology lint is diagnostic only. Dayline remains one `PLAN` object under FIELD; this patch changes the mobile projection and outbound worker handoff, not identity or authority.

## Test

Static regression guards require the one-time mobile PLAIN default, bounded handoff markers, mobile bridge contraction/current-context capture, touch-floor rules and service-worker cache v3. Normal Route Registration + public-surface CI must also pass.

## Stop

Do not add calendar OAuth, a Dayline scheduler, a Hermes daemon, or another planning authority from this patch.

Next evidence should be the phone: can the user open, see what matters, capture reality, mark/run work, and copy a handoff without navigating the atlas first?
