# CROSS-THREAD CONVERGENCE — 2026-09-23

Status: **PUBLIC-SAFE COORDINATION HANDOFF**

This file exists to stop parallel ChatGPT / Hermes / Codex threads from re-deriving different master architectures.

It is **not** a transcript dump, backlog, dashboard, new registry, or canonical store.

Use exact owning artifacts for truth. Use this file only to recover how the current work fits together.

---

## 1. Plain reading

The collaboration is converging toward a small number of durable laws and hosts rather than many peer applications.

```
SOURCE / HOST TRUTH
        ↓
stable identity + address
        ↓
policy / authority
        ↓
bounded transductions / operators
        ↓
unequal projections / domain engines
        ↓
observable result
        ↓
evidence + RETURN
```

The public FIELD repository should contain generic mechanisms, contracts, public artifacts and evidence.

Private personal state belongs in a private local substrate/runtime. The long-horizon private constitution is intentionally **not copied into this public repository**.

The convergence target is therefore **interoperability without authority collapse**:

- keep source/domain truth where it really lives;
- share addressing, projection, bounded transforms and RETURN where they genuinely repeat;
- preserve unequal domain semantics;
- reduce duplicate shells only after behavioral equivalence is proved;
- preserve history/donors even when operational sovereignty is reduced.

---

## 2. Canonical answer by question

| Question | Read this first | Authority |
|---|---|---|
| What matters now? | `/control/CURRENT.json` | current attention / heads |
| What are the global repo rules? | `/AGENTS.md` | collaboration law |
| What is the current INTERPHASE technical model? | `/control/INTERPHASE_SUCCESSOR.json` | technical successor handoff |
| What owns shared transduction/instrument law? | `/control/TRANSDUCTIONS.json` | shared transform/instrument contract |
| How are host facets mapped? | `/control/INTERPHASE_MAPPING_CONTRACT.json` + correspondence registry | mapping contract |
| How should recovery/archaeology return evidence? | `/control/confluence/INTERPHASE_MIGRATION_BACKUP_ARCHAEOLOGY_2026-09-23.md` | recovery recipient contract |
| How should humans/comms be handled? | `/control/confluence/HUMAN_PORT_CONVERGENCE_2026-09-21.md` + `/agent-router.json` + `/contact/contract.json` | communication/policy boundary |
| What is HOUSE/BODY/CARE authority separation? | `/control/BODY_HOUSE_CONVERGENCE_2026-09-21.json` | reality-domain boundary |
| What is the compute/sovereign-node direction? | `/control/confluence/sovereign-node/PORTABLE_AI_SOVEREIGN_NODE_CONVERGENCE_2026-08-26.md` | compute convergence reference |
| What periodic Hermes wardens already exist? | `/control/HERMES_LONG_GOALS.json` | bounded recurring goals |
| What actually happened? | `/returns/` + exact commits/tests | evidence |

If these disagree, inspect the exact current implementation/evidence rather than reconciling by prose.

---

## 3. Current convergence lanes

### A. INTERPHASE / TRANSDUCTIONS

Current shared substrate:

```
HOST
→ CORRESPONDENCE
→ ATTENTION
→ PROJECTION@SCALE
→ OPERATION
→ AUTHORITY / COMMIT
→ EVIDENCE
→ RETURN
```

Core distinction:

**CORRESPOND != PROJECT != OPERATE.**

A projection may suppress declared channels only as explicit residue. A view may not increase authority.

Current technical entrypoint:

`/control/INTERPHASE_SUCCESSOR.json`

### B. Fold/Bloom

Current direction is **one doorway, unequal engines**.

Roles currently read as:

- LISTEN — source/cartography;
- SET — order/seam/weight composition authority;
- TWO DIAL — direct relation/composition controller;
- LIVE — embodied traversal/runtime;
- ECOLOGY — persistent/evolving lineage;
- GLYPH — compressed witness/projection;
- Journey — traversal/seam mechanism and possible future library/debug surface after equivalence proof.

Long-horizon hypothesis:

```
small pure repeated semantic kernel
        ↓
unequal domain adapters / engines
```

Only extract a shared primitive when at least two real consumers demonstrably implement the same semantic law. Do not merge renderers, source clocks, persistence or domain truth for architectural neatness.

### C. READFIELD / Lens / AXIAL

READFIELD, Lens, AXIAL, LISTEN address/aperture mechanics increasingly share substrate, but their domain truth remains unequal.

The current READFIELD × Lens work should be treated as a **bounded INTERPHASE migration/conformance proof**, not a new architecture.

READFIELD remains document/source authority.

### D. Recovery / archive / media

Recovery is a first-class migration lane, not a side archive project.

Preserve raw source, provenance, conflicts, UNKNOWN and anti-merge holds.

Useful recovery output is an addressed packet that another worker can continue from without rereading the entire archive.

Creative/media/research/prompt/story/association material belongs in this corpus when privately available, including:

- stories / narrative fragments / scripts / worlds;
- images / audio / video / interactive media;
- research notes / references / bounded research packets;
- prompts / metaprompts / agent instructions / reusable recipes;
- weak associations and donor/resonance links.

Weak association must not silently become identity, causality or lineage.

### E. HOUSE / HOUSEBUS / BODY / CARE

Private runtime/device truth stays with Home Assistant / HOUSEBUS.

HOUSE owns environment/device/spatial state.

BODY owns private self-observation.

CARE owns episode/course/handoff.

Context may cross these boundaries; authority does not.

Observation != actuation.

MISSING != ZERO.

CONTRAST != CAUSE.

### F. HUMAN PORT / COMMS / agents

Canonical flow:

```
OUTSIDE
→ ADDRESS / PORT
→ INTERPRET
→ POLICY / AUTHORITY
→ TRANSFORM / RESPONSE
→ RETURN
```

Important distinctions:

```
CONTACT != INTERPRETATION != AUTHORITY != ACTIVATION
READ != REMEMBER
DRAFT != SEND
FAMILIARITY != AUTHORITY
URGENCY != AUTHORITY
```

Agent/bot transports are replaceable interfaces, not canonical identity or memory.

### G. Compute / Mac mini / future executor substrate

The desired compute outcome remains boring and recoverable:

```
AUTHORITATIVE CONFIG
→ BOOTSTRAP
→ START
→ VERIFY
→ OPERATE
→ RECOVER
```

Current remembered architecture must not outrank observed machine state.

Prefer files + Git + small deterministic scripts as authority, with Hermes/Codex/model/runtime layers replaceable at the edge.

---

## 4. Current thread/work status

This is a coordination snapshot, not an execution queue.

### In flight / recently handed off

1. **Codex GLYPH conformance to existing TRANSDUCTIONS**
   - Goal: use the existing GLYPH projection/instrument contract, not create a parallel glyph framework.
   - Important falsification already identified: equal member glyphs do not establish equal SET composition because seam/operator state may differ.
   - Until its result lands, other workers should not duplicate this pass.

2. **READFIELD × Lens Studio smallest INTERPHASE migration**
   - Treat as bounded host migration/conformance.
   - Preserve READFIELD source/cursor authority and Lens authority boundaries.
   - Do not turn it into another master theory.

3. **Private Hermes long-horizon coordinator**
   - Private mission spans compute/repo packing, disk/archive recovery and HOUSE/human/automation readiness.
   - Full private-life constitution should stay private/local.
   - Public repo may retain only generic contracts and redacted coordination pointers.

### Preferred technical continuation after current in-flight work

Current INTERPHASE successor still names:

1. SET → LIVE fusion using unified RIDE state;
2. ordinary real-form PAGE/compact/ROOM proof;
3. bounded recovery-packet production;
4. one measured physical MAKE host proof.

Re-evaluate these after current Codex work; do not inherit priority blindly.

---

## 5. Cross-thread logging law

**Do not log chat merely because it happened.**

A thread earns a durable repo change only when it produces one of:

- **DELTA** — changed an existing head;
- **EVIDENCE** — tested/observed an existing claim;
- **DONOR** — recovered a mechanism/source useful to an existing head;
- **RETURN** — recorded what actually happened;
- **UNRESOLVED** — material contribution with no lawful host yet.

For a material cross-thread conclusion, record only:

```
OBJECT / HOST
SOURCE / EVIDENCE
CLAIM OR DELTA
STATUS
AUTHORITY BOUNDARY
AFFECTED EXISTING ARTIFACTS
RESIDUE / CONTRADICTION
NEXT BOUNDED MOVE
RETURN PATH
```

Then:

1. update the owning canonical artifact if reality changed;
2. otherwise add one bounded confluence packet or RETURN;
3. do not copy the same doctrine into several files;
4. do not update FIELD/CURRENT for discussion alone;
5. keep private material outside the public repo.

This file should be amended only when the **cross-thread map itself materially changes**.

---

## 6. Where this is converging to

Not:

- one giant app;
- one universal ontology;
- one database containing a life;
- one dashboard containing every project;
- one renderer for every domain;
- one agent with unlimited authority.

Instead:

```
                    PUBLIC FIELD
generic contracts / code / public artifacts / evidence
                         │
                         │ adapters / redacted contracts
                         ▼
                    PRIVATE FIELD
files / repos / HOUSEBUS / communications / calendar /
archives / media / equipment / people-context / devices
                         │
                         ▼
stable identity + relations + policy + bounded transforms
                         │
             ┌───────────┼───────────┐
             ▼           ▼           ▼
          humans       agents      devices
                         │
                         ▼
                       RETURN
```

The goal is **less duplicated sovereignty and less re-explanation**, while source truth, privacy and domain authority remain explicit.

---

## 7. What is intentionally unresolved

- Whether the Fold/Bloom shared semantic kernel should become a concrete module, and its exact API.
- How much of Journey survives after SET → LIVE equivalence.
- Which READFIELD/Lens shell mechanics can actually be demoted after lived use.
- Which recovered historical interface/MAKE lineages truly correspond versus merely resonate.
- Exact current Mac mini runtime/configuration truth until observed.
- Which private communications/automation flows deserve promotion beyond DRAFT/SHADOW.
- Which HOUSE automations deserve real actuation authority after simulation and measurement.

These are evidence gates, not failures.

---

## 8. Cold-start rule for a new thread

If the new thread is general:

1. read `/control/CURRENT.json`;
2. read `/AGENTS.md`;
3. read this file;
4. follow the domain pointer above;
5. inspect exact implementation/evidence;
6. perform one bounded move;
7. RETURN.

If it is specifically INTERPHASE/Fold-Bloom work, also read:

`/control/INTERPHASE_SUCCESSOR.json`

If it is private-life/compute/HOUSE/comms work, use the private Hermes constitution/workspace and treat this public file only as the generic bridge.

---

## 9. Compact state

**STATE:** convergence is real but distributed across technical, recovery, physical, human and private-runtime lanes.

**DELTA:** this handoff gives those lanes one public-safe map without creating a new state authority.

**EVIDENCE:** current canonical files listed above.

**RESIDUE:** private master constitution and personal source data remain intentionally off-repo.

**WAITING:** results of current Codex GLYPH work; observed private Mac/HOUSE/runtime state where not yet verified.

**ONE NEXT:** after the Codex GLYPH result lands, reconcile that result against TRANSDUCTIONS/INTERPHASE successor before selecting the next implementation front.
