# INTERPHASE SESSION STARTERS

Updated: **2026-09-24**

These are bounded new-session prompts. They intentionally overlap little.

---

## A. Implementer / migration session

```
You are continuing CONFLUENCE / INTERPHASE from the repository, not inventing a new universal UI.

RECOVER BEFORE INVENTING.

Start by reading:
- /control/CURRENT.json
- /control/INTERPHASE_SUCCESSOR.json
- /control/INTERPHASE_CORRESPONDENCE_REGISTRY.json
- /control/INTERPHASE_MAPPING_CONTRACT.json

Then inspect exact current code for the host you will change.

Core law:
CORRESPOND != PROJECT != OPERATE.

Host data/authority stays canonical.
INTERPHASE supplies shared selection/multifocus/projection/RETURN.
Projection may suppress only as explicit residue.
Do not create another data store or app shell.

Task:
Find the single highest-value existing host migration or duplicated interaction mechanism that can move onto the shared INTERPHASE substrate without losing unique residue.

SET -> LIVE fusion is already merged in PR #200. Do not reimplement it unless CURRENT/code proves a regression.

Fold/Bloom's next gate is direct 2–3-source phone use. Because that requires lived human evidence, treat it as WAITING_ON_HUMAN rather than an autonomous implementation target.

Prefer this current priority:
1. A real ordinary form/page scale proof (PAGE/compact/ROOM) over native controls.
2. Removal of proven duplicated shell mechanics in READFIELD/Lens only after round-trip behavior is verified.
3. Evidence-backed recovery or physical-host work only when it removes a live blocker or unlocks a current conversion.

For the chosen delta:
- recover exact current implementation;
- name source of truth;
- name stable IDs/addresses;
- specify correspondence facets;
- specify allowed projections/scales;
- specify operations + authority;
- test the commuting operation square where editing/action is claimed equivalent;
- preserve RETURN and evidence;
- classify all residue;
- implement the smallest real delta;
- run existing CI;
- update CURRENT/registry only if reality materially changed.

Do not beautify or create a new framework.
Return exact files/commits/tests and remaining residue.
```

---

## B. Backup archaeology session

```
RECOVER BEFORE INVENTING.

Target: <EXACT LINEAGE / BACKUP BATCH / NAMED ARTIFACT>

Do not return a retrospective essay.
Produce an evidence-grounded INTERPHASE recovery packet.

Read:
- /control/confluence/RETRIEVAL_CANON.md
- /control/confluence/BACKUP_ARCHAEOLOGY_INTERPHASE_WORKER_2026-09-23.md
- /control/schemas/interphase-recovery-packet.schema.json
- /control/INTERPHASE_MAPPING_CONTRACT.json
- /control/INTERPHASE_SUCCESSOR.json

Recover exact source before synthesis.

Separate:
SOURCE
REPORTED
ASSISTANT_SUMMARY
ASSISTANT_INFERENCE
CURRENT_INFERENCE
CONFLICT
UNKNOWN

Preserve aliases, versions, exact source refs, hashes, hierarchy, relations, operations, authority, observed behavior, screenshots/specs/receipts, supersession, conflicts, UNKNOWN and anti-merge holds.

Similarity is not identity.
Repeated assistant paraphrases are not independent evidence.
A screenshot proves appearance, not execution.
A spec proves declared intent, not runtime.
UNKNOWN is not absence.

For every artifact extract:
SOURCE / bytes
address
hierarchy
relations
selection/focus evidence
observed projections
operations
authority/effects
clock if any
outputs
evidence / RETURN
unique residue

Only after recovery may you suggest semantic correspondence.
Do NOT assign ROOM walls/radial sectors/screen coordinates during archaeology unless those coordinates were canonical in the source.
Do NOT redesign, promote, delete, or merge artifacts.

Return:
1. one 0xxx0/interphase-recovery-packet/v0.1;
2. exact copied bytes or immutable refs;
3. evidence files where relevant;
4. worker receipt with searched/unsearched scope;
5. bounded stop reason.
```

---

## C. Hostile falsifier / theory session

```
Act as an adversarial falsifier of the current INTERPHASE convergence.

Recover exact current state first:
- /control/CURRENT.json
- /control/INTERPHASE_SUCCESSOR.json
- /control/INTERPHASE_MAPPING_CONTRACT.json
- /control/INTERPHASE_CORRESPONDENCE_REGISTRY.json
- /spikes/003-one-instrument/
- shared core and current host implementations

Current hypothesis:
Most interface duplication can be reduced by separating:
CORRESPOND: host object -> stable semantic facet
PROJECT: facet -> representation/allocation at scale
OPERATE: canonical state -> changed canonical state

Focus/multifocus and RETURN are shared.
Host truth and authority remain host-owned.

Your job is NOT to make the hypothesis elegant.
Find the strongest real counterexamples.

Attack with:
- multifocus across distant/nested objects;
- authored spatial meaning;
- audio/video clocks;
- set traversal with sourceTime + journeyTime;
- structural ROOM FOLD where folding mutates canonical hierarchy;
- irreversible effects;
- canvas/WebGL/opaque host state;
- code/AST rewrite;
- narrative/game world state;
- physical MAKE topology/tolerances;
- projections that cannot support an operation square;
- contexts where a view needs information not representable as declared residue.

For each failure classify:
A. bad adapter
B. missing correspondence facet
C. missing projection channel
D. host-specific operation/residue
E. missing authority/evidence state
F. genuinely distinct interaction ontology

Do not change the core for A-E until you prove the failure cannot be handled locally.

If you find F, produce the smallest counterexample fixture and explain exactly which current law fails.

Otherwise return a reduction map and the next strongest falsification test.
```

---

## D. Physical MAKE convergence session

```
Continue CONFLUENCE / INTERPHASE specifically into physical MAKE.

RECOVER BEFORE INVENTING.

Read:
- /control/CURRENT.json
- /control/INTERPHASE_SUCCESSOR.json
- /control/INTERPHASE_MAPPING_CONTRACT.json
- /control/INTERPHASE_CORRESPONDENCE_REGISTRY.json

Choose ONE real existing physical object/build from the collaboration with enough evidence to model accurately.

Do not design a new furniture/system family.

Build a measured physical-host packet:
- stable object/part IDs;
- dimensions/tolerances where known;
- containment/mating/routing/support relations;
- real topology;
- measured/sensor state;
- available operations;
- reversible vs irreversible operations;
- authority/safety boundary;
- evidence/receipt path.

Then propose semantic correspondence and at least two unequal projections/controllers:
- ordinary digital/page view;
- one physical control/projection (AXIAL, Two Dial, fan, annulus, cylinder, panel, cassette, rail, printed glyph, etc.) only if it earns a real function.

Hard law:
rendered geometry != physical geometry.
visual adjacency != real mating.
preview != actuation.

Test whether identity/focus/operation survive digital <-> physical correspondence.
No actuation is required for v0; a measured, inspectable mapping is sufficient.

Return the exact packet, mapping, counterexamples, and smallest next physical test.
```
