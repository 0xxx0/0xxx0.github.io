# APP ATLAS × ATLAS DAYLINE — WRANGLER / SURVEY

Date: 2026-09-25  
Mode: bounded convergence pass  
Surface: `/app-atlas/`

## Refined object

Recover the older App Atlas / Atlas Dayline lineage as a **read-only bearings layer over current CONFLUENCE/FIELD state**, without promoting another control plane.

## What changed

1. Recovered the full 2026-08-09 App Atlas registry (45 records) from the Library into `/recovery/app-atlas/app_atlas_v1.json`.
2. Added `/app-atlas/convergence.json`: a derived OLD→CURRENT crosswalk. Unmapped records remain `ARCHIVE_UNRESOLVED`; absence is not asserted.
3. Added a WRANGLER surface inside the existing IDEA ATLAS:
   - live CURRENT connection;
   - surviving mechanism vectors;
   - explicit Atlas Dayline donor contract;
   - searchable/filterable full historical registry;
   - current-route jump-outs.
4. Existing IDEA ATLAS local capture remains unchanged.
5. Atlas Dayline remains a separate STABLE temporal planning donor.

## High-survival vectors

- **Identity → unequal projection**: DataDisc + ATLAS↔PLAIN → Scale Lens / addressed FIELD.
- **Capture → canon → re-entry**: App Foundry / Living Atlas / Canon Console → IDEA ATLAS + FIELD INDEX + CURRENT/MIGRATION/CENSUS.
- **Lineage → delta**: Thread Tracer / Delta Ledger → Git mutation evidence + RETURN receipts.
- **Evidence firewall**: Seeper / canon discipline → EXACT ≠ RECONSTRUCTED ≠ NEW.
- **Mechanism transfer**: Transfer Bench / Lawful Remix → transduction discipline.
- **Time as projection**: Slothcake / fan/radial → Atlas Dayline; identity stays fixed while reach changes.
- **RETURN**: old Return Engine + Dayline replay → repository-wide compression/execution invariant.
- **Glyph**: compressed projection, never source authority.
- **World**: House OS remains the reality gate; abstraction must convert into observable consequence.

## Atlas Dayline: keep / do not universalize

Keep:
- stable task IDs across ATLAS↔PLAIN;
- anchors + time windows;
- preview ≠ canonical;
- bounded RUN + RETURN/REPLAY;
- local-first export / re-entry;
- plain-list ablation.

Do not universalize:
- its navigation labels as ontology;
- radial geometry where plain is clearer;
- Dayline as repository truth.

## Authority

Current truth remains:
- `/control/CURRENT.json`
- `/showcase-manifest.json`
- `/control/MIGRATION.json`
- `/control/CENSUS.json`

`/app-atlas/convergence.json` is a **derived crosswalk only**.

## Deliberate non-touch

This pass does **not** modify root `index.html`, `showcase-manifest.json`, `CURRENT.json`, or the active FIELD control contract because PRs #241 and #243 currently touch that control plane. Avoiding those paths keeps this branch mergeable and prevents a historical-survey improvement from reopening authority architecture.

## Stop

Stop after the existing App Atlas can answer four questions without a new framework:

1. What did this old thing mean?
2. What mechanism, if any, survived?
3. Where is the current surface/authority?
4. Is the rest retained, parked, retired, or genuinely unresolved?
