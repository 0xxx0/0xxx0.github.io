{
  "schema": "0xxx0/worker-boot/v0.2",
  "updated": "2026-09-25",
  "state": "SUPERSEDED_COMPATIBILITY_POINTER",
  "authority": "NONE",
  "purpose": "Prevent old consumers from treating the Sep-22 generated worker snapshot as live FIELD state.",
  "superseded_by": [
    "/llms.txt",
    "/control/CURRENT.json",
    "/control/QUEUE.json",
    "/showcase-manifest.json",
    "/control/WAITING.json",
    "node scripts/emit-agent-transcript.mjs --json"
  ],
  "machine_handoff": {
    "command": "node scripts/emit-agent-transcript.mjs --json",
    "packet_authority": "NONE",
    "behavior": "Selectively fingerprints live source expressions and intentionally omits live φ focus."
  },
  "laws": [
    "DO NOT USE THIS FILE FOR NOW, HEADS, GATES OR PRIORITY.",
    "A copied worker-state mirror drifts; live authority stays at its owning source.",
    "Transient handoff packets may compress current state but never become canonical stores.",
    "Legacy consumers should follow superseded_by and re-address current focus."
  ],
  "legacy": {
    "prior_schema": "0xxx0/worker-boot/v0.1",
    "prior_snapshot_generated": "2026-09-22T06:38:29Z",
    "disposition": "HISTORICAL_IN_GIT",
    "note": "The previous snapshot carried stale FIELD/READFIELD/FOLD-BLOOM head claims after subsequent merges. Git history preserves it; current file refuses to repeat those claims."
  }
}
