{
  "schema": "field-index-contract/v0.1",
  "updated": "2026-09-20",
  "purpose": "Make FIELD INDEX ordering and activity semantics explicit so repo-writing threads/agents update the same public working index without inflating discussion into progress.",
  "laws": [
    "DISCUSSION ≠ INDEX TOUCH. A chat/thread mention does not change FIELD INDEX ordering by itself.",
    "MUTATION → INDEX TOUCH. Any committed change to a tracked public surface updates that route's index.updated_at in the same bounded repo change.",
    "ACTIVITY MODE ≠ OPERATION. Operation names what the artifact does; work_modes name what kind of work produced the current delta.",
    "UPDATED ORDER ≠ PRIORITY. Root RUNNING defaults to most recently touched tracked surface; FCM/CURRENT own attention priority.",
    "SPECULATION IS LEGIBLE, NOT PROMOTED. SPECULATE/SPECIFY may be indexed without implying implementation or evidence.",
    "RECEIPT WHEN CLAIM CHANGES. If implementation/test/release status changes, update or create the relevant receipt and RETURN entry."
  ],
  "root_order": {
    "default": "UPDATED_DESC",
    "key": "route.index.updated_at",
    "tie_break": "manifest order",
    "alternative": "MANIFEST_ORDER",
    "meaning": "Declared tracked-surface touch time, not filesystem mtime, not browser visit time, and not mere conversation time."
  },
  "activity_modes": {
    "RECOVER": "Retrieve exact prior artifacts/lineage before synthesis.",
    "INVESTIGATE": "Resolve a bounded uncertainty, identity, mechanism or failure through targeted evidence.",
    "RESEARCH": "Gather/synthesize external or corpus evidence; output may be a research note/paper without implementation.",
    "SPECULATE": "Generate explicitly hypothetical/theoretical possibilities; no implementation/evidence claim.",
    "SPECIFY": "Turn an idea/finding into a bounded model, contract, schema or testable design.",
    "IMPLEMENT": "Create or materially change executable/physical artifact state.",
    "VERIFY": "Test, measure, audit or compare a claimed behavior/state.",
    "OPERATE": "Run, coordinate or maintain a live/current system without necessarily changing its design.",
    "RELEASE": "Surface/publish/promote a verified artifact or stable public projection.",
    "UNKNOWN": "Legacy/unclassified activity; valid only during migration."
  },
  "mutation_contract": {
    "required_same_change": [
      "update showcase-manifest route.index.updated_at",
      "set route.index.work_modes to the modes actually exercised by the delta",
      "preserve route.operation independently",
      "update receipt/RETURN only when the claimed artifact/test/release state changed"
    ],
    "do_not_touch_when": [
      "only discussing/brainstorming",
      "reading/reviewing with no durable delta",
      "recovering context but not yet committing recovered state"
    ]
  },
  "ui_contract": {
    "root_running": "show updated order by default; allow manifest-order fallback; display last work_modes and touch date",
    "corner_nav": "shared showcase route adapter remains the canonical BACK/PARENT/SHOWCASE control on tracked non-root routes",
    "style": "presentation/theme controls are a separate cross-site concern and must not alter semantic ordering/state",
    "root_frontier": "Derive BOTTLENECKS live from route.field.exit_paths; never maintain a duplicate hand-written backlog of the same exits."
  },
  "query_semantics": {
    "BOTTLENECK": {
      "status": "EXECUTABLE_ON_ROOT",
      "source": "showcase-manifest route.field.exit_paths",
      "rule": "Return every declared exit whose status is not AVAILABLE. Filtering never changes route or evidence state.",
      "returns": [
        "route",
        "exit class",
        "status",
        "via",
        "physical_fit"
      ],
      "non_claim": "Unresolved exit status exposes interface/proof debt; it is not an automatic priority ranking or evidence that an adapter works."
    }
  },
  "route_state_contract": {
    "law": "route.kind answers what a surface is; route.state answers its current lifecycle/evidence posture. EXPERIMENT remains a valid kind but is deprecated as a route.state.",
    "deprecated_states": [
      "EXPERIMENT"
    ],
    "states": {
      "STABLE": "Verified or settled public surface; ordinary use does not require an outstanding named proof gate.",
      "ACTIVE": "Current runnable/operational head. May still have bounded limitations or unresolved exits.",
      "PROOF_REQUIRED": "Runnable/specifiable surface whose own named promotion or consequence claim is blocked on explicit evidence.",
      "CANDIDATE": "Bounded candidate model/orientation that is intentionally revisable and not canonical.",
      "REFERENCE": "Runnable/reference fixture used to inspect or compare a mechanism; not itself proof of a live adapter or deployment.",
      "PARKED": "Preserved runnable/comparison surface with no current execution obligation; resume only on a named dependency.",
      "DONOR": "Useful surviving mechanism/source whose current role is transfer into another head rather than independent advancement.",
      "FROZEN_DONOR": "Exact recovered donor preserved against in-place modernization; transfer only named mechanisms while keeping source bytes/identity intact.",
      "RECOVER": "Known gap or partial recovery where exact source/evidence is still required.",
      "UTILITY": "Validator/docs/support surface rather than a product/head lifecycle."
    },
    "rules": [
      "Do not use EXPERIMENT as a substitute for uncertainty, lack of proof, parking, donor status or active use.",
      "Put experimental form in route.kind or the release hypothesis/known_limits.",
      "Put unresolved consequence/proof requirements in route.field.exit_paths and PROOF_REQUIRED when they block the artifact's own promotion claim.",
      "Historical release receipts may retain their contemporaneous state; append current classification rather than rewriting provenance."
    ]
  }
}
