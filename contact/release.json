{
  "schema": "release-receipt/v1",
  "artifact": "0 / HUMAN PORT",
  "route": "/contact/",
  "version": "0.4",
  "released": "2026-09-25",
  "state": "STABLE",
  "changes": [
    "promoted /contact/ itself into a first-class shareable stable contact address without adding a server inbox",
    "added Share Contact Link / Copy Contact Link actions with native-share and clipboard fallback",
    "added an explicit optional RETURN VIA field so a sender can preserve the reply route inside the envelope",
    "linked the existing contextual capsule builder for bespoke bearer-visible direct lines without publishing private contact data",
    "retained the local-first composer, PING / REPLY / NEED / CHECK grammar, promise contract and explicit external-action boundary"
  ],
  "checks": [
    "stable contact URL is visible and can be shared or copied without composing a message",
    "RETURN VIA is optional and is preserved only when the sender enters it",
    "message composer still operates locally with no form submit, server endpoint or public message store",
    "native Share falls back to clipboard/prompt and never claims delivery",
    "contextual direct lines remain URL-fragment capsule data rather than repository-committed personal contact data",
    "CHECK mode still states that the page is not monitored or an emergency service"
  ],
  "return": "/contact/",
  "amended": "2026-09-25"
}
