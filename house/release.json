{
 "schema": "release-receipt/v1",
 "artifact": "HOUSE / FIELD",
 "route": "/house/",
 "version": "0.1",
 "released": "2026-09-19",
 "state": "STABLE",
 "changes": [
  "Added /house/ as a public projection of the household control plane: objects, lifecycle counts, capabilities, integration health, uncertainties and receipts.",
  "Added a private-to-public boundary: the projection is generated by a whitelist normalizer in the HOUSEBUS repository and refused when a forbidden pattern is detected.",
  "Published an honest first state while the runtime is not serving: runtime OFFLINE/PARTIAL, inventory AVAILABLE, control UNAVAILABLE, source = HOUSEBUS archaeology.",
  "Kept the nine-card showcase contract, all existing routes, and the AXIAL non-card rule untouched.",
  "Registered the route in the manifest and the RETURN index with a receipt."
 ],
 "checks": {
  "public_surface_validator": "node tools/validate-public.mjs",
  "unit_tests": "node --test tools/*.test.mjs",
  "project_index": "node tools/build-project-index.mjs --check",
  "whitespace": "git diff --check",
  "sanitization": "private generator refuses on forbidden patterns; state.json records result"
 },
 "sources": [
  "/showcase-manifest.json",
  "/house/state.json",
  "/house/contract.json"
 ],
 "honesty": "runtime_health_class reflects a measured probe at generation time and may be OFFLINE while the private runtime is still being brought up."
}
