{
  "schema": "release-receipt/v1",
  "artifact": "HUMAN PORT",
  "route": "/port/",
  "version": "1.3-preview",
  "date": "2026-09-21",
  "state": "INTEGRATION_CANDIDATE",
  "decision": "/port/ is the canonical HUMAN PORT intake/router; /contact/ is one explicit outbound boundary, not the whole Port.",
  "recovered_user_functions": [
    "ADDRESS",
    "INTAKE",
    "INTERPRET",
    "RELATE",
    "HARVEST",
    "PROJECT",
    "RETURN"
  ],
  "visible_routes": [
    "/contact/",
    "/contact/burst/",
    "/app-atlas/",
    "/contact/residue/",
    "/fold-bloom/lens/",
    "/laconic/",
    "/contact/make/",
    "/continuity/",
    "/contact/backstage/"
  ],
  "laws": [
    "CONTACT != ACTIVATION",
    "SOURCE STAYS SOURCE",
    "CONTEXT != AUTHORITY",
    "RETURN WHAT HAPPENED"
  ],
  "promotion_rule": "Keep /contact/ simple. Promote deeper mechanisms onto /port/ only when they reduce real interaction/re-entry cost.",
  "next_tests": [
    "Port sample → BURST arrives as exact RAW",
    "Port source → Contact arrives as local draft and still requires Share/Copy",
    "Port idea → Idea Atlas arrives prefilled but unsaved",
    "Port follow-up → Social Residue arrives prefilled but uncommitted",
    "Port text → Scale Lens parses the same source without a second paste",
    "mobile-width route grid remains usable",
    "CENTER focused CURRENT move → HUMAN PORT arrives preloaded and still requires one explicit route choice"
  ],
  "delta": [
    "replace menu-only home with one local source intake",
    "route the same source explicitly to Contact, BURST, Idea Atlas, Social Residue or Scale Lens",
    "add local session handoffs so target tools arrive preloaded",
    "clarify Contact as HUMAN PORT outbound/share boundary rather than HUMAN PORT itself",
    "retain specialized Laconic, Capsule, Continuity and Backstage tools below the primary router",
    "preserve no-upload / no-auto-send / no-auto-classification boundary",
    "accept bounded same-origin CENTER next-move preload into the canonical intake without granting route/send authority"
  ],
  "first_time_test": "Paste one source once and reach the intended tool preloaded within one explicit route choice."
}
